<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.knu.KNUP.User.IUserDao">

	<!-- <select id="list" resultType="com.knu.KNUP.User.UserDto">
		select * from board order by mId desc
	</select> -->
	
	<update id="insertUser">
	 		insert into knup_user (userId, userEmail, userName) values (#{param1}, #{param2}, #{param3})
	
	</update>
</mapper>