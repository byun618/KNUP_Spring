<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.knu.KNUP.User.IUserDao">

	
	<update id="insertUser">

			 merge into knup_user using dual
			  on (userId = #{param1})
	  			when matched then 
	  				update set userEmail=#{param2}, userName=#{param3}
	  			when not matched then 
	 				insert (userId, userEmail, userName) values (#{param1}, #{param2}, #{param3})
	
	</update>
	
	<update id="deleteUser">
			delete from knup_user
				where userId = #{param1}
	</update>
</mapper>